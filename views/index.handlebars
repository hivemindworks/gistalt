<ul>
{{#each gists}}
  {{#if this.description}}
    <li><a href='/{{ this.id }}'>{{ this.description }}</a></li>
  {{else}}
    <li><a href='/{{ this.id }}'>{{ this.id }}</a></li>
  {{/if}}
{{/each}}
</ul>
