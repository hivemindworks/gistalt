<!doctype html>
<html>
  <head>
    <link rel='stylesheet' type='text/css' href='/css/styles.css'>
  </head>
<body>
  <h1><a href='/'>gistalt.</a></h1>
  {{#if session.accessToken}}
    <a href='/logout'>Logout</a>
  {{else}}
  <a href='/login' class=''>Log in with GitHub</a>
  {{/if}}
  {{{body}}}
</body>
</html>